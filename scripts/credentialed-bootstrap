#!/bin/sh
set -eu

echo "credentialed-bootstrap: repository scripts are not a credential boundary" >&2
echo "use the credential-envelope | sudo /usr/local/bin/deus-aws-bootstrap runtime-service bootstrap protocol in docs/management-seed-runbook.md" >&2
exit 78
