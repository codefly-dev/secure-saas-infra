#!/bin/sh
set -eu

echo "management-seed-access-provisioner: repository scripts are not a credential boundary" >&2
echo "use the credential-envelope | sudo /usr/local/bin/deus-aws-bootstrap runtime-service access-provisioner protocol in docs/management-seed-runbook.md" >&2
exit 78
